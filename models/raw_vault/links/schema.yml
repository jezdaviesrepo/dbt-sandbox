# ************************** SqlDBM: Snowflake *************************
# ************** Generated by SqlDBM: Reigate by jerdavies *************


version: 2

models:
  - name: link_customer_nation
    description: A link table in the raw vault that establishes relationships between customer and nation entities. This table serves as a bridge to capture the many-to-many relationships between customers and nations, supporting historical tracking and data lineage in the data vault architecture.
    config:
      contract:
        enforced: true
    constraints:
      - type: primary_key
        columns:
          - customer_nation_lhk
        name: pk_link_customer_nation
      - type: foreign_key
        columns:
          - customer_hk
        expression: raw_vault.hub_customer (customer_hk)
        name: fk_1
      - type: foreign_key
        columns:
          - nation_hk
        expression: raw_vault.hub_nation (nation_hk)
        name: fk_2
    columns:
      - name: customer_nation_lhk
        data_type: binary(16)
        description: A surrogate key column used as a Link Hub Key (LHK) for identifying unique customer relationships and transactions in the data warehouse. This key enables efficient linking and aggregation of customer-related dimensional and fact data across the analytical system.
        constraints:
          - type: not_null
      - name: customer_hk
        data_type: binary(16)
        description: Unique hash key for identifying a customer in a Data Vault model. This hash key serves as a surrogate key that provides a consistent and unique identifier for customer records across different source systems, ensuring data integration and avoiding duplicate customer entries.
      - name: nation_hk
        data_type: binary(16)
        description: Hashed key representing the nation or country identifier in a Data Vault model, used for tracking and linking nation-related information across different tables and entities.
      - name: load_date
        data_type: timestamp_ltz(9)
        description: ''
      - name: record_source
        data_type: varchar(23)
        description: Identifies the source system or origin of the data record, tracking the provenance and initial point of data entry